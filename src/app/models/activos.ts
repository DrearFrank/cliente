import { Title } from '@angular/platform-browser';

export interface Activo {
    id_activos?: number,
    id_categoria?: number,
    nombre?: string,
    descripcion?: string,
    serie?: string,
    cod_utn?: string,
    estado?: string,
    cantidad?: string
};